# PIX-004.6 — Resolução de Acesso e Preparação Preview

**Data:** 2026-09-17 • **Branch:** `main` • **Commit base:** `1933882` • **Supabase ref:** `cycqigdywekfwwaspsus` (Projeto Alfa Web) • **Supabase CLI:** `2.109.1` • **Vercel CLI:** `59.1.3` • **PIX_ENABLED=false**

## 1. Supabase — Autenticação

- **Comando:** `supabase orgs list` → `nmxqpiemcxjeancqrlql | Face a Face` (única org visível).
- **Comando:** `supabase projects list` → `vdnnrmqhyzjxwuzhbjza | Alfa Racing Brasil` e `lxuisnhznfuvroqrqrii | Face a Face` — **não lista** `cycqigdywekfwwaspsus`.
- **Arquivo:** `supabase/.temp/linked-project.json` → `{"ref":"cycqigdywekfwwaspsus","organization_id":"oruvptvlozdatobanzoj"}` — projeto pertence a `oruvptvlozdatobanzoj`, diferente de `nmxqpiemcxjeancqrlql` do token atual.
- **Resultado:** `npx supabase migration list --linked` e `npx supabase db lint --linked` → `403 Your account does not have the necessary privileges` (inicialização do login role).
- **Ação necessária:** `supabase login --token ***` com token gerado por conta membro de `oruvptvlozdatobanzoj` (não colar no chat). Até lá, lint remoto **bloqueado** (documentado, não contornado com `db push`).

## 2. Projeto Supabase confirmado (sem expor segredo)

- **Ref correto:** `cycqigdywekfwwaspsus` (linked-project.json).
- **Acesso atual:** **sem leitura** (`403`) com token `Face a Face`.
- **Permissão necessária:** `project:read` + `db:lint` para `oruvptvlozdatobanzoj`.

## 3. Vínculo Vercel

- **Comando:** `vercel link --project gpro-alfa-racing-brasil --yes` (já executado em 004.5) → `✓ Created joelson-goncalves-projects/gpro-alfa-racing-brasil` + `Connected GitHub Joegonn/gpro-alfa-racing` + `Updated .env.local`.
- **Confirmação:** `Get-Content .vercel/project.json` → `{"projectId":"prj_ZhbjTdeew8fbrrsPIhERTDQbiCbH","orgId":"team_ecUTQWNGP2O4BZZ610XGu6eC","projectName":"gpro-alfa-racing-brasil"}`.
- **Usuário:** `vercel whoami` → `joelgonn-4089`.
- **Projeto correto:** `joelson-goncalves-projects/gpro-alfa-racing-brasil` (team `joelson-goncalves-projects`).
- **Ambientes:** `vercel env ls` → `No Environment Variables found` (Preview **e** Production vazios).

## 4. Variáveis Vercel auditadas (nomes e ambientes, sem valores)

| Variável | Preview | Production | Local `.env.local` |
|---|---|---|---|
| `PIX_ENABLED` | **ausente** | **ausente** (deve permanecer `false`) | `false` (`Get-Content .env.local` → `PIX_ENABLED=***` `false`) |
| `MERCADOPAGO_ACCESS_TOKEN` | **ausente** | **ausente** | ausente (`Select-String` `0` no `app/`) |
| `MERCADOPAGO_WEBHOOK_SECRET` | **ausente** | **ausente** | ausente |
| `MERCADOPAGO_WEBHOOK_SIGNATURE_TEMPLATE` | **ausente** | **ausente** | ausente |

**Regra:** `PIX_ENABLED` só `true` em **Preview** após `MERCADOPAGO_*` em `supabase secrets` + `vercel env add preview` (não `production`).

## 5. Domínio Preview

- **Comando:** `vercel ls` → `No deployments found under joelson-goncalves-projects`.
- **Comando:** `vercel domains ls` → `0 Domains found` (nenhum domínio custom).
- **Domínio esperado:** `https://gpro-alfa-racing-brasil-<hash>-joelson-goncalves-projects.vercel.app` (gerado no primeiro `git push` para `main` após `vercel link`; ainda não existe porque `git push` não foi feito nesta sprint).
- **Rota webhook:** `app/api/payments/webhooks/mercadopago/route.ts` `force-dynamic` existe em `npm run build` `44/44` (`ƒ /api/payments/webhooks/mercadopago`). URL completa será `https://<preview>.vercel.app/api/payments/webhooks/mercadopago` (POST, `401` sem `x-signature`).

## 6. Lint remoto

- **Comando:** `npx supabase db lint --linked` → `403` (mesmo token).
- **Comando:** `npx supabase migration list --linked` → `403`.
- **Estado anterior (quando token tinha acesso):** `No schema errors found` (PIX-004.3). Não revalidado por falta de permissão — **bloqueio documentado**.
- **Impacto:** `tyres_suppliers` `20260918000001` já aplicada (`migration list` anterior mostrava `Local|Remote` `20260918000001|20260918000001`), mas não pôde ser reconfirmada.

## 7. Validações locais

```powershell
git diff --check          # 0
npx tsc --noEmit          # 0
node --test tests/pix-0011-webhook.test.js tests/pix-002-plans.test.js tests/pix-002-security.test.js # 3 pass, 986 checks
npm run build             # 44/44 (ƒ /planos, ƒ /api/payments/webhooks/mercadopago)
```

**Produção protegida:** `app/planos/page.tsx:50` `pixEnabled ? <CheckoutButton> : <button disabled> Pagamento indisponível` + `app/api/payments/webhooks/mercadopago:93` `if verified !== 'verified' → 401` (fail-closed).

## 8. Bloqueios restantes

1. **Supabase:** `SUPABASE_ACCESS_TOKEN` com `oruvptvlozdatobanzoj` (org do projeto) — sem isso, `migration list`/`db lint` permanecem `403`.
2. **Vercel:** `vercel env add` para `Preview` sem `Production`.
3. **Deploy Preview:** `git push` para `main` necessário para gerar `*.vercel.app` (depende de autorização).

**Não houve:** `supabase db push`, `db reset`, `UPDATE premium_plans`, `PIX_ENABLED=true`, pagamento, `commit`/`push` ( `git log --oneline -1` ainda `1933882`).

**Próxima ação:** `supabase login --token ***` (conta `oruvptvlozdatobanzoj`) → `supabase migration list --linked` → `No schema errors` → `vercel env add MERCADOPAGO_* preview` + `PIX_ENABLED true preview` **só após** `UPDATE premium_plans SET is_active=true WHERE code='vip_monthly'` em Preview DB com `TEST-` plano.

*Sem commit/push/db push nesta sprint.*
