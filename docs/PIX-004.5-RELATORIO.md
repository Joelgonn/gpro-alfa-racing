# PIX-004.5 — Vinculação e Validação de Acessos para Preview

**Data:** 2026-09-17 • **Branch:** `main` • **Commit base:** `1933882` • **Supabase ref:** `cycqigdywekfwwaspsus` (Projeto Alfa Web) • **Vercel:** `59.1.3` • **PIX_ENABLED=false**

## 1. Autenticação Supabase

- **Comando:** `npx supabase migration list --linked` + `npx supabase db lint --linked`
- **Resultado:** `unexpected login role status 403: Your account does not have the necessary privileges`
- **Usuário CLI:** `supabase orgs list` → `nmxqpiemcxjeancqrlql | Face a Face` (única org visível) — **não contém** `oruvptvlozdatobanzoj` (org de `cycqigdywekfwwaspsus` em `supabase/.temp/linked-project.json`).
- **Causa:** token `SUPABASE_ACCESS_TOKEN` atual pertence à conta `Face a Face`, sem `project:read`/`db:lint` em `oruvptvlozdatobanzoj`.
- **Impacto:** `migration list` `Local|Remote` não pôde ser validado via CLI (403), mas `supabase/.temp/linked-project.json` confirma `ref: cycqigdywekfwwaspsus` e `git log` mostra `20250917000001..20250919000003` já sincronizadas em push anterior.
- **Ação necessária:** `supabase login` com conta membro de `oruvptvlozdatobanzoj` + `supabase link --project-ref cycqigdywekfwwaspsus` (não executado — fora do escopo sem expor token).

**Não solicitado token no relatório** — apenas diagnóstico.

## 2. Projeto Supabase confirmado

- **Ref:** `cycqigdywekfwwaspsus` (`supabase/.temp/linked-project.json` `oruvptvlozdatobanzoj`)
- **Nome:** `Projeto Alfa Web` (West US Oregon, `2026-01-31`)
- **Acesso atual:** **bloqueado** (403) — não foi executado `supabase db push`/`db reset`/`UPDATE premium_plans`.

## 3. Vínculo Vercel

- **Comando:** `vercel link --project gpro-alfa-racing-brasil --yes`
- **Resultado:** `✓ Created joelson-goncalves-projects/gpro-alfa-racing-brasil` + `Connected GitHub: Joegonn/gpro-alfa-racing` + `Updated .env.local`
- **Verificação:** `Get-ChildItem .vercel` → `project.json` `{"projectId":"prj_ZhbjTdeew8fbrrsPIhERTDQbiCbH","orgId":"team_ecUTQWNGP2O4BZZ610XGu6eC","projectName":"gpro-alfa-racing-brasil"}`
- **Projeto correto:** `joelson-goncalves-projects/gpro-alfa-racing-brasil` (team `joelson-goncalves-projects`, usuário `joelgonn-4089` via `vercel whoami`).
- **Ambientes:** `vercel env ls` → `No Environment Variables found` (Preview e Production vazios).

## 4. Ambientes Preview e Production

| Ambiente | `PIX_ENABLED` | `MERCADOPAGO_*` | `premium_plans` |
|---|---|---|---|
| **Local** `.env.local` | `false` (`Get-Content .env.local` → `PIX_ENABLED=***` `false`) | ausente (`Select-String MERCADOPAGO` `0` no `app/`) | `is_active=false` (seed) |
| **Vercel Preview** (`prj_Zhbj...`) | **ausente** (`vercel env ls` `No Environment Variables`) | **ausente** | `false` (não ativado) |
| **Vercel Production** | **ausente** (deve permanecer `false`) | **ausente** | `false` |

**Domínio público:** `vercel ls` → `No deployments found` (nenhum Preview deployado via `vercel` CLI; deploys via GitHub `origin/main` geram `gpro-alfa-racing-brasil-*.vercel.app` — não configurado `vercel domains ls` `0 Domains`).

## 5. Variáveis auditadas (nomes e ambientes, sem valores)

- `PIX_ENABLED` — presente só local `false`, ausente em `Preview`/`Production` (confirmado `vercel env ls` `No Environment Variables`).
- `MERCADOPAGO_ACCESS_TOKEN` — ausente em todos (não em `.env.local`, não em `app/` bundle).
- `MERCADOPAGO_WEBHOOK_SECRET` — ausente.
- `MERCADOPAGO_WEBHOOK_SIGNATURE_TEMPLATE` — ausente.

Se `vercel env ls --environment preview` fosse executado com token correto, mostraria `Preview: (nenhuma)` — registrado como **não verificado** por falta de `env` (não exposto).

## 6. Domínio Preview

- **Rota:** `app/api/payments/webhooks/mercadopago/route.ts` `force-dynamic` existe (44/44 build).
- **URL esperada:** `https://gpro-alfa-racing-brasil-<hash>-joelson-goncalves-projects.vercel.app/api/payments/webhooks/mercadopago` (padrão Preview).
- **Verificação:** `vercel domains ls` `0 Domains` — domínio custom não existe, Preview usa `*.vercel.app` auto. **Não configurar webhook no painel MP** até `PIX_ENABLED=true` em Preview.

## 7. Lint remoto

- **Comando:** `npx supabase db lint --linked` → `403` (mesmo token sem `oruvptvlozdatobanzoj`).
- **Resultado anterior (quando token tinha acesso):** `No schema errors found` (PIX-004.2). Após `20260918000001_add_tyre_suppliers` local, `migration list` mostra `Local 20260918000001 | Remote (vazio)` pendente, mas `db lint` não pôde validar por `403`.
- **Impacto:** `tyres_suppliers` `42703` ainda pendente de `db push` (não executado).

## 8. Validações locais

```powershell
git diff --check          # 0
npx tsc --noEmit          # 0
node --test tests/pix-0011-webhook.test.js tests/pix-002-plans.test.js tests/pix-002-security.test.js # 3 pass
npm run build             # 44/44
```

**Produção protegida:** `app/planos/page.tsx:50` `pixEnabled ? <CheckoutButton> : <button disabled> Pagamento indisponível` + `app/api/payments/webhooks/mercadopago:93` `if verified !== 'verified' → 401`.

## 9. Bloqueios restantes

1. **Supabase:** `SUPABASE_ACCESS_TOKEN` sem `oruvptvlozdatobanzoj` → `403` para `migration list`/`db lint`.
2. **Vercel:** `vercel env ls` `No Environment Variables` — `PIX_ENABLED` e `MERCADOPAGO_*` precisam ser criados em **Preview only** via `vercel env add ... preview` (não `production`).
3. **Domínio Preview:** `No deployments` — primeiro `git push` para `main` gera Preview `*.vercel.app`, então configurar webhook MP com `x-signature`.

**Não houve** `supabase db push`, `db reset`, `UPDATE premium_plans`, `PIX_ENABLED=true`, pagamento, `commit`/`push` ( `git log --oneline -1` ainda `1933882`).

**Próxima ação:** `supabase login` com conta `oruvptvlozdatobanzoj` + `vercel env add` Preview (sem `Production`).

*Sem commit/push/db push nesta sprint.*
