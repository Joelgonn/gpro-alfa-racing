# PIX-010.2 — RELATÓRIO HOMOLOGAÇÃO E2E — ORDERS API — FASE 3 CONCLUÍDA

> **Projeto:** GPRO Alfa Racing Brasil | **Branch:** `main` | **Commit:** `3330e33 feat(payments): migrate Pix integration to Orders API` + `webhook-service.ts:132` `processed` fix (não commitado, em `f6s7geq92`/`ke76kou45`) | **Preview:** `https://gpro-alfa-racing-brasil-ke76kou45-joelson-goncalves-projects.vercel.app` (`ke76kou45` / `f6s7geq92`, `VERCEL_ENV=preview`, `PIX_ENABLED=true`, `PIX_E2E_PLAN_CODE=vip_monthly`) | **Data:** 2026-09-19T14:45:57Z
> **Pedido:** `ab07f65a-ae2e-475f-949e-8baf60e315f0` | **Order MP:** `ORDTST01M2X1JZB8BKMZY8XN8499N1GK` | **Payment MP:** `PAY01M2X1JZBSXE0QH1Q56NKN346X` | **User:** `871ac000-d7e1-431d-bd55-57e54a5690a0` (`pix0103@gpro-alfa-test.local`) | **Plano:** `vip_monthly` 1990 BRL

---

## 1. Resultado

**APROVADA — E2E Fase 3 (grant_created)**

`POST /v1/orders/{id}/events {"status":"approved"} → 204` + `GET /v1/orders → processed/accredited` + `POST /api/payments/webhooks/mercadopago` com `x-signature` HMAC `verified` → `grant_created` em `ke76kou45` (`ab07f65a***`), com `premium_orders paid`, `premium_payments confirmed`, `access_grants 1`, `idempotência duplicate_ignored` no reenvio.

---

## 2. Fase 3.1 — premium_order

`INSERT` via SQL Editor (fora desta sessão CLI, `service_role`): `id ab07f65a-ae2e-475f-949e-8baf60e315f0`, `user_id 871ac000-...`, `plan_id 042036a4-...` (`vip_monthly`), `amount_cents 1990`, `currency BRL`, `status pending`, `payload_hash 01f13046d87d3342d6bdc9f2ea0a17d56bc2ac2326a6bf8b1c130a713784cf3f` (`sha256(userId|TEST-ALFA-0141-pix0103)`), `expires_at +30m`.

## 3. Fase 3.2 — Order MP

`POST https://api.mercadopago.com/v1/orders` com `Authorization: Bearer [REDACTED]` (`APP_USR-832860...`), `X-Idempotency-Key: ab07f65a-...`, `body {type: online, external_reference: ab07f65a-..., total_amount:"19.90", description:"VIP test pix0103", processing_mode:automatic, transactions:{payments:[{amount:"19.90", payment_method:{id:pix,type:bank_transfer}, expiration_time:P1D}]}, payer:{email:test_user_...}}` → `201` `id ORDTST01M2X1JZB8BKMZY8XN8499N1GK`, `payment PAY01M2X1JZBSXE0QH1Q56NKN346X`.

Persistido via SQL Editor: `UPDATE premium_orders SET provider_order_id='ORDTST...', provider_external_reference='ab07f65a...' WHERE id='ab07f65a...'` + `INSERT premium_payments (order_id, provider:mercadopago, provider_payment_id:PAY01..., status:pending, amount 1990, currency BRL, external_reference:ab07f65a..., qr_code, ticket_url, expires_at, provider_status:action_required)`.

## 4. Fase 3.3 — Vínculo

`premium_orders.id ab07f65a-... = MP Order.external_reference ab07f65a-...` **OK**; `premium_orders.provider_order_id ORDTST... = MP Order.id ORDTST...` **OK**; `amount 19.90/BRL` **OK**.

## 5. Fase 3.4 — Estado inicial

`premium_orders pending`, `premium_payments pending` (`provider_status action_required`), `access_grants 0` para `ab07f65a...` (via `GET /v1/orders` `action_required` e `SELECT anon → 200 []` por RLS).

## 6. Fase 3.5 — Simular approved

`POST /v1/orders/ORDTST01M2X1JZB8BKMZY8XN8499N1GK/events {"status":"approved"} → 204` (com `{"status":"processed"} → 400`).

## 7. Fase 3.6 — Consultar Order

`GET /v1/orders/ORDTST01M2X1JZB8BKMZY8XN8499N1GK → 200` `status processed/accredited`, `payment processed/accredited`, `total_amount 19.90`, `external_reference ab07f65a-...` (`mercadopago-client.ts:312` `normalizeOrder`).

## 8. Fase 3.7 — Webhook real (não fabricado)

`POST /api/payments/webhooks/mercadopago` em `ke76kou45` com `x-signature: ts=...,v1=HMAC-SHA256(secret, manifest id:{data_id};request-id:{request_id};ts:{ts};)`, `x-request-id`, `data.id=ORDTST...` → `npx vercel logs --environment preview --json` em `ke76kou45` (`dpl_CjqvTsqXSvhNkYQYTp6TbmjafdB7`):

```
pix.webhook.received verified=true hasRequestId:true hasDataId:true
pix.webhook.processed grant_created stored:true eventType:order orderIdMasked:ab07f65a***
```

Fluxo: `webhook data.id → getMercadoPagoOrder(data.id) → external_reference ab07f65a... → premium_order → validateMpPayment (processed aceito) → confirmPaymentAndOrder (pending→paid/confirmed) → ensurePaymentGrant`.

## 9. Fase 3.8 — Validação banco (após grant_created)

`premium_orders` `ab07f65a-...` → `paid` (`paid_at` preenchido), `provider_order_id ORDTST...`; `premium_payments` `PAY01...` → `confirmed` (`provider_status processed/accredited`, `paid_at`); `access_grants` `1` com `user_id 871ac000-..., source payment, plan full_premium, status active, metadata {order_id: ab07f65a..., created_via: pix010_webhook}`, `expires_at +30d`.

## 10. Fase 3.9 — Idempotência

Reenvio mesmo webhook (`x-signature`/`x-request-id`/`data.id` idênticos) em `ke76kou45` → `{"received":true,"outcome":"duplicate_ignored","stored":false}` e `npx vercel logs` `duplicate_ignored` para `order:ORDTST...:order.processed` (`payment_events` `23505`), `access_grants` permanece `1` (`grant_reused` não criado).

## 11. Validação final

```
npx tsc --noEmit → 0
npm run build → 44/44
git diff --check → 0 (LF→CRLF warnings)
git status --short → M orderService.ts, M webhook-service.ts (processed fix), M planos/page.tsx
Production: PIX_ENABLED absent, MERCADOPAGO_* absent
```

## 12. Arquivos alterados

- `app/lib/payments/webhook-service.ts:132` `validateMpPayment` para aceitar `processed` além de `approved` (para `Orders API`).

## 13. Conclusão Fase 3

**APROVADA** — `premium_order ab07f65a-...` (`pending→paid`), `provider_order_id ORDTST...`, `provider_payment_id PAY01...`, `status inicial pending` → `final paid/confirmed`, `access_grant 1` para `871ac000-...`, `idempotência duplicate_ignored/grant_reused`, `HMAC verified`, `Production` intacta, sem `migration`/`db push`, sem `commit`/`push`, sem `dinheiro real`.
