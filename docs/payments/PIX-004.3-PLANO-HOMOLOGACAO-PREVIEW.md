# PIX-004.3 — Plano de Homologação Controlada (Preview)

> **Projeto:** `gpro-alfa-racing-brasil` | **Branch:** `main` | **Commit base:** `1933882` | **Supabase ref:** `cycqigdywekfwwaspsus` | **PIX_ENABLED=false** | **Build 44/44**

---

## 1. Auditoria de configuração de ambientes

| Variável | Uso no código | Local correto | Valor atual (mascarado) |
|---|---|---|---|
| `PIX_ENABLED` | `app/planos/page.tsx:50` `process.env.PIX_ENABLED === 'true'` (server-only) | Vercel **Preview** `true` para homologação, **Production** `false` (manter), `.env.local` `false` | `PIX_ENABLED=***` (`false`) |
| `MERCADOPAGO_ACCESS_TOKEN` | `app/lib/payments/mercadopago-client.ts` (server-only, nunca `NEXT_PUBLIC`) | Vercel Preview + `supabase secrets` (não `.env.local`) | Não em `app/` bundle — `Select-String MERCADOPAGO` `0` no cliente |
| `MERCADOPAGO_WEBHOOK_SECRET` | `app/lib/payments/mercadopago-signature.ts` `SIGNATURE_SECRET_ENV` | Vercel Preview + Supabase Vault | Não em `app/` |
| `MERCADOPAGO_WEBHOOK_SIGNATURE_TEMPLATE` | Mesmo arquivo `SIGNATURE_TEMPLATE_ENV` (`id:[data.id];request-id...`) | Vercel Preview | Não em `app/` |
| `NEXT_PUBLIC_SUPABASE_URL` / `ANON_KEY` | `supabaseAdmin` server | Vercel Preview/Production + `.env.local` | `***` |
| `SUPABASE_SERVICE_ROLE_KEY` | `app/lib/supabase-admin.ts` `server-only` | Vercel Preview/Production (server) | `***` |

**Verificado:** `Get-Content .env.local | Select-String PIX` → só `PIX_ENABLED=***` (`false`), sem `MERCADOPAGO_*` em `.env.local` — correto. `supabase secrets list --project-ref cycq***us` não executado (evita expor), mas `app/` não importa `MERCADOPAGO_` no cliente (`Select-String` `0`).

## 2. Ambiente correto

| Ambiente | `PIX_ENABLED` | `MERCADOPAGO_*` | `premium_plans.is_active` | Webhook URL |
|---|---|---|---|---|
| **Local** (`.env.local`) | `false` | **não** | `false` (seed `false`) | — |
| **Vercel Preview** (`vercel --prod` **não**) | `true` **só após** secrets + plano teste ativo | `TEST-` `APP_USR-` sandbox | `true` só para `vip_monthly` teste | `https://<preview>.vercel.app/api/payments/webhooks/mercadopago` |
| **Vercel Production** | `false` (manter) | **não** | `false` | — |
| **Supabase remoto** (`cycq***us`) | — | `supabase secrets` (Vault) | `false` | — |

**Não ativar `PIX_ENABLED` em Production.** Não alterar `.env.local` com segredos reais.

## 3. Pré-requisitos (bloqueantes)

- [ ] **Access Token sandbox** `APP_USR-` / `TEST-` do vendedor de teste (Mercado Pago Dashboard → Teste → Credenciais).
- [ ] **Segredo de assinatura** do webhook (Painel MP → Webhooks → `x-signature` `v1=` HMAC).
- [ ] **Template de assinatura** (ex: `id:[data.id];request-id:[x-request-id];ts:[ts];`) — `MERCADOPAGO_WEBHOOK_SIGNATURE_TEMPLATE`.
- [ ] **URL pública Preview** (ex: `https://gpro-alfa-racing-brasil-git-pix-*.vercel.app/api/payments/webhooks/mercadopago`) — obter após `vercel --prebuilt` **sem** `PIX_ENABLED`.
- [ ] **Conta teste compradora** (MP → Teste → Usuários de teste) e **vendedora** (mesma conta do token).
- [ ] **Plano inativo** confirmado: `SELECT code, is_active FROM premium_plans` → `false` para `vip_monthly`/`vip_lifetime` (seed `false`).

**Não solicitar tokens no chat** — usar `supabase secrets set` e `vercel env add` com `***`.

## 4. Fluxo de ativação segura

1. **Planos inativos por padrão** — `supabase/seed.sql` `INSERT ... is_active=false ON CONFLICT DO NOTHING` (não `true`). Confirmado `Get-Content supabase/seed.sql` `is_active false`. Nenhum `UPDATE premium_plans SET is_active=true` sem autorização.
2. **`PIX_ENABLED` permanece `false`** até Preview preparado — `app/planos/page.tsx:50` ternário `pixEnabled ? <CheckoutButton> : <button disabled> Pagamento indisponível` — sem `PIX_ENABLED`, nenhum `POST /api/payments/orders` é chamado.
3. **Webhook inválido não concede** — `app/api/payments/webhooks/mercadopago/route.ts:93` `if verified !== 'verified' → 401` (fail-closed), `payment_events` dedupe `hashPayload` `64 hex`, `UNIQUE event_id`.
4. **Pagamento não aprovado não concede** — `webhook-service.ts` só `grant` se `payment.status === 'approved'` e `order.status === 'paid'` (hoje `premium_payments.status` `pending` → não `confirmed`).
5. **Duplicidade** — `access_grants` `uniq_grant_payment_order` `(metadata->>'order_id') WHERE source='payment'` (migration `20250919000002`) impede 2 grants do mesmo `order_id`.

## 5. Roteiro de homologação (Preview, sem Production)

**1. Configuração de secrets (Preview only):**
```powershell
vercel env add MERCADOPAGO_ACCESS_TOKEN preview
# colar APP_USR- sandbox (não logar)
vercel env add MERCADOPAGO_WEBHOOK_SECRET preview
vercel env add MERCADOPAGO_WEBHOOK_SIGNATURE_TEMPLATE preview
# ex: id:[data.id];request-id:[x-request-id];ts:[ts];
supabase secrets set MERCADOPAGO_ACCESS_TOKEN=*** --project-ref cycqigdywekfwwaspsus
```

**2. Variável Preview:**
```powershell
vercel env add PIX_ENABLED true preview
```

**3. Ativação controlada de **um** plano de teste (após secrets):**
```sql
-- Supabase SQL Editor (Preview DB cycq***us)
UPDATE premium_plans SET is_active=true, price_cents=1990 WHERE code='vip_monthly';
-- Verificar
SELECT code, is_active, price_cents FROM premium_plans;
```

**4. `PIX_ENABLED` só em Preview:** `vercel env ls | Select-String PIX_ENABLED` deve mostrar `preview true`, `production false`.

**5. Criação de pedido de teste (Preview, usuário de teste):**
- Login com **conta teste compradora** em `https://<preview>.vercel.app/login` → `/planos` → `vip_monthly` → `Checkout` → `POST /api/payments/orders` `{planCode:"vip_monthly"}` + `Idempotency-Key: TEST-...` → `201` `order.status pending` + `payment.status created` (sem `grant`).

**6. Webhook:** MP envia `POST https://<preview>.vercel.app/api/payments/webhooks/mercadopago` com `x-signature: v1=***` + `x-request-id` + body `{data:{id:"TEST-123"}}` → `verifySignature` `verified` (com `secret`+`template`) → `payment_events` `stored=true` `outcome not_verified→verified`.

**7. Validação de assinatura:** sem `x-signature` → `401 signature_not_verified` (já testado `pix-0011-webhook` `not_verified`); `x-signature` inválida → `401 mismatch`; sem segredo/template → `not_verified` (fail-closed).

**8. Idempotência:** `POST` webhook duplicado `event_id` igual → `duplicate_ignored` ( `UNIQUE event_id` ), sem segundo `grant`.

**9. Conferência tabelas:**
```sql
SELECT count(*) FROM payment_events; -- 1
SELECT status, amount_cents FROM premium_orders; -- pending 1990
SELECT status FROM premium_payments; -- pending (não confirmed)
SELECT count(*) FROM access_grants WHERE source='payment'; -- 0 (PIX_ENABLED preview ainda não concede até approved)
```

**10. Abortamento:** qualquer `grant` com `PIX_ENABLED=false` ou `payment.status != approved` → `vercel env rm PIX_ENABLED preview` + `UPDATE premium_plans SET is_active=false`.

**11. Desativação:** `vercel env rm MERCADOPAGO_ACCESS_TOKEN preview` + `UPDATE premium_plans SET is_active=false` + `vercel --prod` sem `PIX_ENABLED`.

## 6. Validações permitidas (sem alteração de dados)

```powershell
git diff --check
npx tsc --noEmit
node --test tests/pix-0011-webhook.test.js tests/pix-002-plans.test.js tests/pix-002-security.test.js
npm run build
npx supabase migration list --linked
npx supabase db lint --linked
```

**Não executar:** `supabase db push` / `db reset --linked` / `UPDATE premium_plans` / `PIX_ENABLED=true` / pagamento sandbox / `supabase secrets set` sem autorização / `commit`/`push`.

## 7. Resultado esperado

- `supabase migration list --linked` `Local|Remote` `16|16` (já `No schema errors found`)
- `supabase db lint --linked` `No schema errors` (após `tyre_suppliers` `20260918000001`)
- `PIX_ENABLED=false` em `Production` e `.env.local`, `true` só em `Preview` após passo 2.

**Próxima ação:** aguardar `MERCADOPAGO_ACCESS_TOKEN` sandbox (`APP_USR-`) + `MERCADOPAGO_WEBHOOK_SECRET` via `supabase secrets`/`vercel env` Preview, então executar roteiro 1-9 com `TEST-`.

*Sem commit/push/db push nesta sprint.*
