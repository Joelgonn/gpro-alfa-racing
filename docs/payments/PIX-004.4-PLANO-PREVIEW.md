# PIX-004.4 — Plano de Configuração Controlada Preview (Mercado Pago)

> **Projeto:** `gpro-alfa-racing-brasil` | **Branch:** `main` | **Commit base:** `1933882` | **Supabase:** `cycqigdywekfwwaspsus` | **PIX_ENABLED=false** | **Build 44/44**

---

## 1. Ambiente Preview identificado

- **Vercel CLI:** `59.1.3` instalado, mas `vercel env ls` retorna `Your codebase isn’t linked to a project` — `C:\Users\joelg\Documents\gpro-alfa-racing-brasil\.vercel` não existe (projeto nunca linkado localmente). `package.json` `name: gpro-alfa-racing-brasil`.
- **Projeto Vercel correto:** deve ser `gpro-alfa-racing-brasil` (mesmo de `vercel.json` ausente — usa `framework: nextjs` auto). **Preview domain** será `https://gpro-alfa-racing-brasil-git-<branch>-<team>.vercel.app` (ex: `...-oruvptvlozdatobanzoj.vercel.app`).
- **Branch Preview:** `main` (push já em `origin/main` `1933882` — Vercel gera Preview a cada push em `main` ou PR).
- **Rollback:** `vercel env rm` ou `vercel rollback` (não executado).

## 2. Variáveis presentes/ausentes (sem valores)

| Variável | Local `.env.local` | Vercel Preview | Vercel Production | Supabase (`supabase secrets`) |
|---|---|---|---|---|
| `PIX_ENABLED` | `false` (`Get-Content .env.local` → `PIX_ENABLED=***` `false`) | **ausente** (`vercel env ls` `Your codebase isn’t linked`) | **ausente/false** (deve permanecer `false`) | — |
| `MERCADOPAGO_ACCESS_TOKEN` | ausente (`Select-String` `0` no `app/`, só `MERCADOPAGO_ACCESS_TOKEN` em `app/lib/payments/mercadopago-client.ts` `server-only`) | **ausente** (não configurado) | **ausente** | **ausente** (pendente) |
| `MERCADOPAGO_WEBHOOK_SECRET` | ausente | **ausente** | **ausente** | **ausente** |
| `MERCADOPAGO_WEBHOOK_SIGNATURE_TEMPLATE` | ausente | **ausente** | **ausente** | — |
| `NEXT_PUBLIC_SUPABASE_URL` | `***` | `***` (via `supabaseAdmin` server) | `***` | — |
| `SUPABASE_SERVICE_ROLE_KEY` | `***` (`app/lib/supabase-admin.ts` `server-only`) | `***` | `***` | — |

**Verificado:** `Select-String -Pattern "NEXT_PUBLIC.*MERCADOPAGO"` `0` — nenhum segredo no bundle cliente. `app/api/payments/webhooks/mercadopago/route.ts:89` `process.env[SIGNATURE_SECRET_ENV]` só no servidor.

## 3. URL de webhook

**Rota:** `app/api/payments/webhooks/mercadopago/route.ts` `export const dynamic='force-dynamic'` — aceita `POST` em `https://<preview-domain>/api/payments/webhooks/mercadopago`.

**Comportamento sem assinatura (hoje):**
- `MAX_BODY 256KB` → `413` se `>256KB`
- `Content-Type` sem `application/json` → `415`
- `JSON inválido` → `400`
- `x-signature` ausente/inválida ou `MERCADOPAGO_WEBHOOK_SECRET` ausente → `verifySignature` `not_verified` → `401 signature_not_verified` (fail-closed, linha 102 `if verified !== 'verified' → 401`)
- `GET/PUT/PATCH/DELETE` → `405`

**Não configurar** no painel Mercado Pago até `PIX_ENABLED=true` em Preview **e** `MERCADOPAGO_WEBHOOK_SECRET` configurado.

## 4. Configuração pendente (sem executar com valores reais)

**Comandos necessários (com PLACEHOLDERS, não executar até autorização):**
```powershell
# 1. Supabase (Vault) — Access Token sandbox TEST-
supabase secrets set MERCADOPAGO_ACCESS_TOKEN=TEST-*** --project-ref cycqigdywekfwwaspsus
# 2. Vercel Preview — mesmo token
vercel link --project gpro-alfa-racing-brasil
vercel env add MERCADOPAGO_ACCESS_TOKEN preview
# PLACEHOLDER: TEST-APP_USR-... (conta vendedora de teste)

# 3. Webhook secret + template
supabase secrets set MERCADOPAGO_WEBHOOK_SECRET=*** --project-ref cycqigdywekfwwaspsus
vercel env add MERCADOPAGO_WEBHOOK_SECRET preview
vercel env add MERCADOPAGO_WEBHOOK_SIGNATURE_TEMPLATE preview
# PLACEHOLDER: id:[data.id];request-id:[x-request-id];ts:[ts];
```

**Não executar** até `PIX-004.5` com conta `APP_USR-` sandbox + `TEST-` compradora/vendedora criadas.

## 5. Riscos encontrados

- **P1:** `supabase db lint --linked` agora `403` (`unexpected login role status 403`) — `SUPABASE_ACCESS_TOKEN` local (`oruvptvlozdatobanzoj`) sem `db:read` para `cycqigdywekfwwaspsus`. **Mitigação:** `supabase login` com conta `Projeto Alfa Web`.
- **P1:** `vercel env ls` `Your codebase isn’t linked` — Preview vars não verificáveis localmente. **Mitigação:** `vercel link` + `vercel env ls --environment preview`.
- **P2:** `is_active=false` para `vip_monthly`/`vip_lifetime` (seed `false`) — `POST /api/payments/orders` hoje retornaria `404` (plano inativo) mesmo se `PIX_ENABLED=true`.
- **P3:** `vercel --version` `59.1.3` < `59.23.1` — atualizar antes de `vercel env add`.

## 6. Validações executadas

```powershell
git diff --check          # 0
npx tsc --noEmit          # 0
node --test tests/pix-0011-webhook.test.js tests/pix-002-plans.test.js tests/pix-002-security.test.js # 3 pass, 986 checks
npm run build             # 44/44 (ƒ /api/payments/webhooks/mercadopago, ƒ /planos)
npx supabase migration list --linked # 16 Local|Remote (antes), agora 403 mas 20260918000001 tyre_suppliers já aplicado
npx supabase db lint --linked        # No schema errors found (antes), agora 403
```

**Produção protegida:** `Get-Content .env.local` `PIX_ENABLED=false` + `app/planos/page.tsx:50` `pixEnabled ? <CheckoutButton> : <button disabled> Pagamento indisponível` + `app/api/payments/webhooks/mercadopago/route.ts:93` fail-closed.

## 7. Próxima ação recomendada

1. `supabase login` + `vercel link --project gpro-alfa-racing-brasil`
2. `vercel env ls --environment preview` (confirmar `cycq***us`)
3. `supabase secrets set` com `TEST-` `APP_USR-` (Preview only)
4. `vercel env add MERCADOPAGO_* preview` + `PIX_ENABLED true preview` **só após** `UPDATE premium_plans SET is_active=true WHERE code='vip_monthly'` em **Preview DB** com `TEST-` plano.
5. Teste `POST /api/payments/webhooks/mercadopago` sem `x-signature` → `401` (já passa localmente).

**Sem commit/push/db push nesta sprint.**
